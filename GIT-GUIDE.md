# Git 提交和推送指南

## 📋 基本 Git 工作流程

### 1️⃣ 查看当前状态

```bash
git status
```

查看哪些文件被修改、添加或删除。

### 2️⃣ 添加文件到暂存区

#### 添加所有更改的文件
```bash
git add .
```

#### 添加特定文件
```bash
git add _config.yml
git add index.md
```

#### 添加特定目录
```bash
git add _posts/
git add assets/
```

### 3️⃣ 提交更改

```bash
git commit -m "提交说明信息"
```

**提交信息示例**：
```bash
git commit -m "优化博客 URL 路径结构"
git commit -m "添加新的博客文章"
git commit -m "更新首页设计"
git commit -m "修复样式问题"
```

### 4️⃣ 推送到远程仓库

```bash
git push origin main
```

如果是第一次推送，可能需要设置上游：
```bash
git push -u origin main
```

## 🚀 完整示例流程

### 场景：修改了博客配置并添加了新文章

```bash
# 1. 查看状态
git status

# 2. 添加所有更改
git add .

# 3. 提交
git commit -m "更新博客配置并添加新文章"

# 4. 推送到 GitHub
git push origin main
```

## 📝 常用 Git 命令

### 查看信息
```bash
# 查看状态
git status

# 查看提交历史
git log --oneline -10

# 查看文件差异
git diff

# 查看已暂存的文件
git diff --staged
```

### 撤销操作
```bash
# 撤销工作区的修改（未 add）
git checkout -- 文件名

# 撤销暂存区的文件（已 add，未 commit）
git reset HEAD 文件名

# 修改最后一次提交信息
git commit --amend -m "新的提交信息"
```

### 分支操作
```bash
# 查看分支
git branch

# 创建新分支
git branch 分支名

# 切换分支
git checkout 分支名

# 创建并切换分支
git checkout -b 分支名
```

## 🔄 从远程仓库更新

```bash
# 拉取最新更改
git pull origin main

# 或者先获取再合并
git fetch origin
git merge origin/main
```

## ⚠️ 常见问题

### 1. 推送被拒绝（需要先拉取）

```bash
# 先拉取远程更改
git pull origin main

# 解决冲突后再次推送
git push origin main
```

### 2. 忘记添加文件

```bash
# 如果已经提交但忘记添加文件
git add 文件名
git commit --amend --no-edit
git push origin main
```

### 3. 提交信息写错了

```bash
# 修改最后一次提交信息
git commit --amend -m "正确的提交信息"
git push origin main --force  # 注意：谨慎使用 force
```

## 🎯 最佳实践

### ✅ 好的提交习惯

1. **频繁提交**
   - 完成一个小功能就提交一次
   - 不要积累太多更改

2. **清晰的提交信息**
   - ✅ "添加文章分类功能"
   - ✅ "优化首页布局"
   - ❌ "更新"
   - ❌ "修改"

3. **提交前检查**
   ```bash
   git status
   git diff
   ```

4. **提交前测试**
   - 本地运行 `bundle exec jekyll serve` 测试
   - 确认没有错误再提交

### 📦 提交前检查清单

- [ ] 运行 `git status` 查看更改
- [ ] 运行 `git diff` 确认修改内容
- [ ] 本地测试网站是否正常
- [ ] 检查是否有临时文件（如 `.DS_Store`）
- [ ] 确认提交信息清晰明确

## 🔐 首次设置（如果需要）

### 配置用户信息

```bash
git config --global user.name "您的名字"
git config --global user.email "your-email@example.com"
```

### 检查配置

```bash
git config --list
```

## 📚 快速参考

| 操作 | 命令 |
|-----|------|
| 查看状态 | `git status` |
| 添加文件 | `git add .` |
| 提交 | `git commit -m "信息"` |
| 推送 | `git push origin main` |
| 拉取 | `git pull origin main` |
| 查看历史 | `git log --oneline` |
| 查看差异 | `git diff` |

## 🎉 现在开始

### 快速三步提交

```bash
# 1. 添加所有更改
git add .

# 2. 提交
git commit -m "您的提交说明"

# 3. 推送
git push origin main
```

---

**提示**：如果这是您第一次使用 Git，建议先熟悉基本命令。如有问题，随时询问！

